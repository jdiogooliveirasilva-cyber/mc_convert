{
  "name": "mcworld-converter",
  "version": "1.0.0",
  "private": true,
  "description": "Conversor de Mundos Bedrock - transforma pastas de mundo do Minecraft Bedrock em arquivos .mcworld, preservando Behavior/Resource Packs.",
  "scripts": {
    "build": "npm install --prefix client && npm install --prefix server && npm run build --prefix client && npm run build --prefix server",
    "start": "npm start --prefix server"
  }
}
